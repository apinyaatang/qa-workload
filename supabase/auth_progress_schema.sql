-- ============================================================================
--  ⛔  ห้ามรันไฟล์นี้  —  ถูกแทนที่แล้ว
-- ============================================================================
--
--  เนื้อหาเดิมของไฟล์นี้ยังไม่เคยถูกรัน และมีบั๊ก 5 จุดที่ทำให้ระบบใช้งานไม่ได้:
--
--    1. การเช็คสิทธิ์ admin 7 จุด query ตาราง profiles จาก policy ที่อยู่บน
--       profiles เอง → recursion อ่านตารางนั้นไม่ได้ตั้งแต่ query แรก
--    2. trigger ตอนสมัครอ่าน role จาก raw_user_meta_data ที่ client ส่งมาเอง
--       → ใครสมัครได้ก็ขอสิทธิ์ admin ได้ในคำขอเดียว
--    3. ไม่มีคอลัมน์ is_active → ปิดการใช้งาน user ไม่ได้
--    4. planning_id เป็น uuid แต่ planning id คือ business key ที่เป็น text
--       → การส่ง progress ล้มทุกครั้ง
--    5. ไม่มีอะไรกันการล็อก admin คนสุดท้าย
--
--  และที่สำคัญที่สุด: ไฟล์นี้ไม่ได้แก้ปัญหาที่ policy ของตารางเดิมทั้งหมด
--  ผูกอยู่กับ role `anon` — การเปิด login โดยไม่แก้จุดนั้นก่อนจะทำให้แอปพัง
--
--  เวอร์ชันที่แก้แล้วและลำดับการรันที่ปลอดภัยอยู่ที่:
--
--      supabase/migrations/          ← รันตามลำดับเลขไฟล์
--      docs/AUTH_ROLLOUT.md          ← ลำดับ deploy และวิธีกู้ระบบ
--
--  เนื้อหาเดิมดูได้จาก git history:
--      git log --follow -p -- supabase/auth_progress_schema.sql
-- ============================================================================

DO $$
BEGIN
  RAISE EXCEPTION
    E'ไฟล์นี้ถูกแทนที่แล้ว — ใช้ supabase/migrations/ ตามลำดับเลขไฟล์\n'
     'ดูขั้นตอนที่ docs/AUTH_ROLLOUT.md';
END $$;
